---
description: Fix compile then run and fix tests
---

Build and fix `$ARGUMENTS` (or current target if none specified).

1. Build target
2. Fix compile errors (repeat until clean)
3. Run tests
4. Fix test failures (repeat until passing)
